package sqlspanner_test

import (
	// . "github.com/tcncloud/sqlspanner"

	. "github.com/onsi/ginkgo"
	// . "github.com/onsi/gomega"
)

var _ = Describe("Conn", func() {
	Describe("given a database path", func() {
		It("should connect to the db", func() {
			// conn =
			// Expect(actual, extra)
		})
	})
})
